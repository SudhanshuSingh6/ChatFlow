package com.chatflow.gateway;

import org.junit.jupiter.api.Test;
import org.springframework.boot.test.context.SpringBootTest;

/** Proves the gateway boots: route definitions + edge auth filter wire up (no downstream needed). */
@SpringBootTest
class ChatflowGatewayApplicationTests {

    @Test
    void contextLoads() {
    }
}
